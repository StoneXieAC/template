#include<bits/stdc++.h>

#define rep(i, a, b) for(int i = a; i <= (b); ++i)
#define frep(i, a, b) for(int i = a; i < (b); ++i)
using namespace std;
typedef long long ll;
const int MOD = 1e9 + 7;

signed main() {
    do {
        system("maker.exe");
        system("test.exe");
        system("std.exe");
    } while (!system("fc test.out std.out"));
    return 0;
}
